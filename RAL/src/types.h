#pragma once

#include <iostream>
#include <vector>
#include <string>

using i8_t = signed char;
using u8_t = unsigned char;
using i8vec_t = std::vector<char>;
using u8vec_t = std::vector<unsigned char>;

using i16_t = short;
using u16_t = unsigned short;
using i16vec_t = std::vector<short>;
using u16vec_t = std::vector<unsigned short>;

using i32_t = int;
using u32_t = unsigned int;
using i32vec_t = std::vector<int>;
using u32vec_t = std::vector<unsigned int>;

using i64_t = long long;
using u64_t = unsigned long long;
using i64vec_t = std::vector<long long>;
using u64vec_t = std::vector<unsigned long long>;

using f32_t = float;
using f32vec_t = std::vector<float>;

using f64_t = double;
using f64vec_t = std::vector<double>;




